@5
D = A
@0
M = D

// for loop, store a constant n somewhere. Loop n times, starting at ram[m] and each time moving to ram[m+1]
// if jump, the value in A is stored to the PC and that will be the line read next (?)